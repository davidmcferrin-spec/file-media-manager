Options -Indexes

RewriteEngine On

# Allow direct access to real files/dirs (vendor assets, etc.)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route everything else through front controller
RewriteRule ^ index.php [QSA,L]
